---
import { getCollection } from 'astro:content';

export async function getStaticPaths() {
  const posts = await getCollection('posts');
  return posts.map(post => ({
    params: { slug: post.slug },
    props: { post },
  }));
}

const { post } = Astro.props;
const { Content } = await post.render();
---

<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{post.data.title} - yhna941.dev</title>
  <meta name="description" content={post.data.description}>
  
  <!-- KaTeX CSS for math rendering -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.css" integrity="sha384-n8MVd4RsNIU0tAv4ct0nTaAbDJwPJzDEaqSD1odI+WdtXRGWt2kTvGFasHpSy3SV" crossorigin="anonymous">
  
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    :root {
      --bg: #121212;
      --bg-card: #1e1e1e;
      --text: #ececec;
      --text-secondary: #acacac;
      --border: #2d2d2d;
      --accent: #12b886;
      --accent-hover: #20c997;
      --code-bg: #282c34;
      --inline-code-bg: rgba(18, 184, 134, 0.15);
      --inline-code: #63e6be;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Apple SD Gothic Neo', 'Pretendard', 'Roboto', 'Noto Sans KR', 'Segoe UI', sans-serif;
      background: var(--bg);
      color: var(--text);
      line-height: 1.8;
    }

    .container {
      max-width: 768px;
      margin: 0 auto;
      padding: 0 1rem;
    }

    .back-link {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      margin: 2rem 0;
      color: var(--text-secondary);
      text-decoration: none;
      font-size: 0.9rem;
      transition: color 0.2s;
    }

    .back-link:hover {
      color: var(--accent);
    }

    header {
      padding: 2rem 0;
    }

    h1 {
      font-size: 2.5rem;
      font-weight: 800;
      line-height: 1.3;
      margin-bottom: 1.5rem;
      word-break: keep-all;
    }

    .post-meta {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      color: var(--text-secondary);
      font-size: 0.9rem;
      margin-bottom: 1.5rem;
    }

    .tags {
      display: flex;
      gap: 0.5rem;
      flex-wrap: wrap;
      margin-bottom: 2rem;
    }

    .tag {
      background: rgba(18, 184, 134, 0.1);
      color: var(--accent);
      padding: 0.375rem 0.75rem;
      border-radius: 16px;
      font-size: 0.875rem;
      font-weight: 500;
      border: 1px solid rgba(18, 184, 134, 0.2);
    }

    .divider {
      height: 1px;
      background: var(--border);
      margin: 2rem 0 3rem;
    }

    article {
      font-size: 1.0625rem;
      padding-bottom: 4rem;
    }

    article h1,
    article h2,
    article h3,
    article h4 {
      margin-top: 2.5rem;
      margin-bottom: 1rem;
      font-weight: 700;
      line-height: 1.4;
    }

    article h1 { font-size: 2rem; }
    article h2 { 
      font-size: 1.75rem;
      padding-bottom: 0.5rem;
      border-bottom: 2px solid var(--border);
    }
    article h3 { font-size: 1.5rem; }
    article h4 { font-size: 1.25rem; }

    article h2:first-child,
    article h3:first-child {
      margin-top: 0;
    }

    article p {
      margin-bottom: 1.75rem;
      word-break: keep-all;
    }

    article ul,
    article ol {
      margin-left: 1.75rem;
      margin-bottom: 1.75rem;
    }

    article li {
      margin-bottom: 0.75rem;
    }

    article li > p {
      margin-bottom: 0.5rem;
    }

    article code {
      background: var(--inline-code-bg);
      color: var(--inline-code);
      padding: 0.2rem 0.5rem;
      border-radius: 4px;
      font-size: 0.9em;
      font-family: 'Fira Code', 'SF Mono', Monaco, Consolas, monospace;
      font-weight: 500;
    }

    article pre {
      background: var(--code-bg);
      padding: 1.5rem;
      border-radius: 8px;
      overflow-x: auto;
      margin-bottom: 2rem;
      border: 1px solid var(--border);
      line-height: 1.6;
    }

    article pre code {
      background: none;
      color: #abb2bf;
      padding: 0;
      font-size: 0.9rem;
      font-weight: 400;
    }

    article blockquote {
      border-left: 4px solid var(--accent);
      padding-left: 1.25rem;
      margin: 1.75rem 0;
      color: var(--text-secondary);
      background: rgba(18, 184, 134, 0.05);
      padding: 1rem 1.25rem;
      border-radius: 4px;
    }

    article blockquote p:last-child {
      margin-bottom: 0;
    }

    article a {
      color: var(--accent);
      text-decoration: none;
      border-bottom: 1px solid transparent;
      transition: border-color 0.2s;
    }

    article a:hover {
      border-bottom-color: var(--accent);
    }

    article hr {
      border: none;
      border-top: 1px solid var(--border);
      margin: 3rem 0;
    }

    article strong {
      font-weight: 700;
      color: var(--text);
    }

    article img {
      max-width: 100%;
      height: auto;
      border-radius: 8px;
      margin: 2rem 0;
    }

    /* KaTeX (Math) styles */
    article .katex {
      font-size: 1.1em;
    }

    article .katex-display {
      margin: 2rem 0;
      overflow-x: auto;
      overflow-y: hidden;
    }

    article .katex-display > .katex {
      white-space: normal;
    }

    footer {
      text-align: center;
      padding: 3rem 0;
      color: var(--text-secondary);
      font-size: 0.875rem;
      border-top: 1px solid var(--border);
    }

    footer a {
      color: var(--accent);
      text-decoration: none;
    }

    footer a:hover {
      color: var(--accent-hover);
    }

    /* Scrollbar */
    ::-webkit-scrollbar {
      width: 8px;
      height: 8px;
    }

    ::-webkit-scrollbar-track {
      background: var(--bg);
    }

    ::-webkit-scrollbar-thumb {
      background: var(--border);
      border-radius: 4px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: #404040;
    }
  </style>
</head>
<body>
  <div class="container">
    <a href="/" class="back-link">← 목록으로</a>

    <header>
      <h1>{post.data.title}</h1>
      <div class="post-meta">
        <span>
          📅 {post.data.pubDate.toLocaleDateString('ko-KR', {
            year: 'numeric',
            month: 'long',
            day: 'numeric'
          })}
        </span>
        <span>•</span>
        <span>✍️ {post.data.author}</span>
      </div>
      <div class="tags">
        {post.data.tags.map((tag: string) => (
          <span class="tag">#{tag}</span>
        ))}
      </div>
    </header>

    <div class="divider"></div>

    <article>
      <Content />
    </article>

    <footer>
      <p>Built with Astro 🚀 • <a href="https://github.com/yhna941" target="_blank">GitHub</a></p>
    </footer>
  </div>
</body>
</html>
